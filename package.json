{
  "name": "ucs-web",
  "version": "1.0.0",
  "description": "Unified Cloud Sensors website — exec demo",
  "scripts": {
    "start": "eleventy --serve",
    "build": "eleventy"
  },
  "devDependencies": {
    "@11ty/eleventy": "^3.0.0"
  }
}
